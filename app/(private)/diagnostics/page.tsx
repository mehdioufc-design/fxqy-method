import { DiagnosticsClient } from "@/components/management/diagnostics-client";

export const dynamic = "force-dynamic";

export default function DiagnosticsPage() {
  return <DiagnosticsClient />;
}

